@model IEnumerable<moment3.Models.AuthorModel>

@{
    ViewData["Title"] = "Författare";
}

<h2 class="mb-4">📖 Författare</h2>

<p>
    <a asp-action="Create" class="btn btn-success mb-3">Lägg till ny författare</a>
</p>

<div class="table-responsive">
    <table class="table table-striped table-hover table-bordered">
        <thead class="table-dark">
            <tr>
                <th style="width: 80%;">@Html.DisplayNameFor(model => model.Name)</th>
                <th style="width: 20%;" class="text-center">Åtgärder</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Name)</td>
                    <td class="text-center">
                        <div class="d-flex flex-column">
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm w-auto mb-2">Redigera</a>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm w-auto mb-2">Detaljer</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm w-auto">Ta bort</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>